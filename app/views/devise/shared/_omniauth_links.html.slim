- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to t("devise.views.links.omni_auth", provider: provider.to_s.titleize), omniauth_authorize_path(resource_name, provider)
    br/
